name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: fissible/.github/.github/workflows/test-bash.yml@main
    with:
      test-command: bash tests/ptyunit/run.sh
